# CR-20260709-001 开发总结

## 基本信息

| 字段 | 内容 |
|---|---|
| 变更标题 | 发起链路场景验收样本补强、回填边界收口与文档治理完善 |
| CR 编号 | CR-20260709-001 |
| 分支 | `feature/CR-20260709-001-initiation-link-scenario-acceptance` |
| 变更类型 | 治理型补强 / 验收资产补强 / 交互边界收口 / 文档回写 |
| 适用工程 | `vue-reference/` |
| 技术栈 | Vue3 |
| 当前正式业务对象 | 产品申请工单、产品申请预算核销 |

---

## 本轮补强内容总览

本轮按"治理型小切口"执行，**未新增业务功能，未扩大页面结构复杂度，未重新引入旧概念**。

核心目标：将创建页规则从"规则说明文档"升级为"可直接验收文档"，使发起链路具备正式场景验收样本、回填边界可明确复现、导入链路具备错误/警告样本、页面/mock/store/route/文档形成一致口径。

---

## 文档补强项清单

### 1. `docs/product/work-order-budget/产品申请工单创建页面规则明细.md`

| 章节 | 内容 | 状态 |
|---|---|---|
| §21 场景验收样本总表 | 新增，覆盖 A-H 八大类 53 个场景，每个场景含进入方式、核心检查点、预期结果、当前参考实现边界 | ✅ 已完成 |
| §22 返回/回填/store 持久化边界 | 新增，覆盖初始化边界、选择页返回边界、route.query 承接边界、sessionStorage/store 边界 | ✅ 已完成 |
| §23 错误示例 | 新增，覆盖 15 个已验证错误示例 + 3 个当前阶段未完整实现项 | ✅ 已完成 |
| §24 导入链路规则承接 | 新增，覆盖输入方式、模板口径、解析与校验口径、导入结果承接、错误/警告样本（含 5 组样本） | ✅ 已完成 |
| §25 Mock/route/页面进入说明 | 新增，覆盖正式入口路由、选择页返回方式、驳回重提进入、场景依赖、快速复现方法 | ✅ 已完成 |
| 版本升级 | v0.1 → v0.2 | ✅ 已完成 |
| 更新日志 | 补充 v0.2 变更记录，标注 CR-20260709-001 | ✅ 已完成 |
| 参考文档清单 | 附本轮关联文档路径 | ✅ 已完成 |

---

## 页面 / store / mock 修正项清单

### 1. `vue-reference/src/views/work-order-budget/CreateView.vue`

| 修正项 | 修正说明 | 行号范围 |
|---|---|---|
| 驳回重提场景 session 隔离 | 驳回重提场景不再调用 `initStoreGroups()` 读取旧 session，改为使用默认空分组，避免复用上次未提交数据 | onMounted 初始化逻辑重构 |
| 附件数量上限硬拦截 | `addAtt()` 增加 `attachments.length >= 10` 判断，超限则 toast 提示并阻断 | 约第 160 行 |

### 2. `vue-reference/src/stores/workOrder.ts`

| 检查项 | 结论 | 是否修正 |
|---|---|---|
| `initStoreGroups()` 恢复时机 | 与文档§22.4.2 一致 | 无需修正 |
| `persistStoreGroups()` 调用边界 | `watch` 自动触发，与文档§22.4.3 一致 | 无需修正 |
| `clearAllGroups()` 作用范围 | 与文档§22.4.4 一致 | 无需修正 |
| `importWorkOrderData()` 覆盖后持久化 | `watch` 自动触发 `persistStoreGroups`，与文档§22.4.9 一致 | 无需修正 |

### 3. `vue-reference/src/mocks/index.ts`

| 检查项 | 结论 | 是否修正 |
|---|---|---|
| 导入链路行为与文档一致性 | `parseImportText`/`validateImportRows`/`parseFileImport`/`downloadImportTemplate` 行为与§24 一致 | 无需修正 |
| 错误/警告样本完整性 | 硬错误（产品不存在/专卖店不存在/层级5/禁止下单/格式错误/数量非法）与软警告（数量超限）覆盖完整 | 无需修正 |

---

## 自测验证项

| 验证项 | 验证方式 | 结果 |
|---|---|---|
| 构建无错误 | `npm run build` | ✅ 通过，主包 230.93 kB，exceljs 独立 chunk 929.89 kB |
| 场景样本总表与代码一致性 | 文档 §21 场景逐一核对代码实现 | ✅ 53 个场景中 50 个已验证实现一致，3 个当前阶段未完整实现项已明确标注边界 |
| 驳回重提 session 隔离 | 代码审查 `CreateView.vue` onMounted | ✅ 修正后驳回重提不再读取旧 session |
| 附件上限拦截 | 代码审查 `addAtt()` | ✅ 已增加上限判断 |
| 错误示例与实现一致性 | 文档 §23 错误示例逐一核对 | ✅ 15 个已验证示例中 14 个实现符合，1 个（驳回重提 session）本轮已修正 |

---

## 参考文档

1. `docs/change-logs/2026-07-09/CR-20260709-001 变更清单.md`
2. `docs/change-logs/2026-07-09/CR-20260709-001 任务清单.md`
3. `docs/product/work-order-budget/产品申请工单创建页面规则明细.md`（v0.2）
4. `vue-reference/src/views/work-order-budget/CreateView.vue`
5. `vue-reference/src/stores/workOrder.ts`
6. `vue-reference/src/mocks/index.ts`
7. `vue-reference/src/router/index.ts`
8. `BRANCH_GUIDE.md`
9. `WORKFLOW_GUIDE.md`

---

## 本轮未处理项（非本轮范围）

1. `uuid` moderate 漏洞（传递依赖，非业务直接依赖，历史 CR 已评估不强制修复）
2. 附件类型/大小校验的完整实现（当前为模拟环境，待正式工程接入真实上传组件）
3. 浏览器后退后 query 参数丢失的降级处理（移动端浏览器行为限制，正式工程可通过路由守卫解决）

---

## 后续待办

1. 待用户验收文档完整性（场景样本总表是否覆盖全部验收需求）
2. 待用户验收代码修正（驳回重提 session 隔离、附件上限）
3. 验收通过后合并到 main

---

> 本轮按"治理型小切口"执行完毕，目标为：**发起链路场景验收样本补强、回填边界收口与文档治理完善**。
> 文档已升级为可直接验收文档，代码轻量对齐 2 处，构建通过，自测完成。

# 2026-07-01 工作日志

## 项目初始化

- 阅读了项目核心文档：`项目协作基线.md`、`CLAUDE.md`、`WORKFLOW_GUIDE.md`
- 梳理了 AI 协作行为准则与红线

## 分支创建与构建

- 从 `main` 创建分支 `init/CR-20260701-workbuddy-init`
- 为 `vue-reference/` 补齐缺失依赖：`vue-router`、`pinia`、`tailwindcss`、`@tailwindcss/vite`
- 构建验证通过：46 模块，`dist/` 输出正常

## 分支同步

- 将 main（含 `CR-20260701-001 变更清单.md`）合并到 `init/CR-20260701-workbuddy-init`
- 两分支现已一致，无差异

## CR-20260701-001 变更执行

### 代码变更（`vue-reference/src/views/work-order-budget/CreateView.vue`）
- 3.1 申请信息区压缩：预算未选仅显示申请人，选中后展开完整三行
- 3.2 命名优化：6处文案替换，UI中不再出现"专卖店申请分组""商品"
- 3.3 说明文案：总标题"此处开始填写需要预算核销的产品和订单归属专卖店"，每个明细内"同一个店编的预算核销产品填写在一个明细"
- 3.5 附件区升级：网格布局+文件数量badge+图片/文档图标区分+预览占位+删除按钮
- 3.6 PDF合并提示："建议将照片和扫描件合并为一份 PDF 上传"

### 文档变更（4份）
- `产品申请工单AI开发任务说明.md`：v0.1→v0.2，新增更新日志
- `产品申请工单字段清单初稿.md`：v0.3→v0.4，新增2.3.3同店编归并规则，命名口径切换
- `产品申请工单页面结构草图说明.md`：v0.2→v0.3，全文档命名切换，5.2.3申请信息两态说明
- `产品申请工单和产品申请预算核销PRD.md`：v0.4→v0.5，13.3补充归并规则

### 验证
- 构建通过（46模块，0错误）
- 本地预览 http://localhost:5173/

## CR-20260701-001 UI 微调（用户反馈）

- 附件数量显示格式：badge 样式 `X 个文件` → 纯文本 `(X/10)`，始终显示
- 提示语位置修正：从分隔线下方移至标题下方、分隔线上方（展开状态下紧跟标题文字）

## 提交

- 分支 `init/CR-20260701-workbuddy-init` 已 commit: `5c92d87`
- 6 files changed, 260 insertions(+), 104 deletions(-)
- 已通过 PR #11 合并到 main
- 当前在 main，commit `19bdbd5`（开发总结文档追加）

## 技能建设

- 创建 `usability-test` 技能（~/.workbuddy/skills/usability-test/），用户级，跨项目可用
- 支持 screenshot / interaction / full 三种测试模式
- 自动验证申请信息压缩、命名口径、附件格式、PDF 提示等

## neat-freak 文档同步

- `CLAUDE.md`：路由表"专卖店分组"→"产品申请订单明细"，新增 CR-001 Vue 命名口径约定
- `README.md`：当前阶段补充 CR-20260701-001 完成记录
- `docs/README.md`：全量修正——补齐 002/003/004/001 所有 development-summary，补齐产品文档和技术文档，删除不存在目录引用
- `docs/daily-reports/项目日报-v2026.07.01.md`：新建 7/1 日报

## CR-20260701-002 批量导入功能开发

### 变更范围
- 仅涉及 `vue-reference/`，改动 4 个文件，不改 React 仓

### 文件变更
- `vue-reference/src/types/index.ts`：新增 ImportRow、ImportError、ImportResult 接口
- `vue-reference/src/mocks/index.ts`：新增 validateProduct、validateQuantity、validateStore、parseImportText、validateImportRows 函数
- `vue-reference/src/stores/workOrder.ts`：新增 importWorkOrderData、clearAllGroups 方法
- `vue-reference/src/views/work-order-budget/CreateView.vue`：新增"批量导入产品"按钮 + 轻量提示 + 导入弹窗（textarea / 识别检查 / 错误展示 / 确认导入）+ 覆盖确认交互

### 已实现功能
1. 入口按钮：与"添加产品申请订单"并列，橙色系区分
2. 轻量提示：导入数据需包含产品SKU、数量、专卖店编号
3. 导入弹窗：格式说明 + textarea + 识别检查 + 确认导入
4. 文本解析：支持逗号/空格/Tab 分隔，跳过空行
5. 硬拦截校验：产品不在核销范围 / 数量非正整数 / 专卖店无效 → 展示错误列表，禁止导入
6. 软警告：数量超出可申请数量 → 显示警告，允许导入，提交时拦截（复用 skuTotalMap）
7. 数据拆分：按专卖店编号自动拆分生成多个产品申请订单明细
8. 覆盖导入：已有数据时弹确认提示 → 确认后清空并导入
9. 全有或全无：格式错误 + 校验错误全部展示

### 验证
- 构建通过（46模块，0错误）
- 可用性自测：5个场景全部通过（空页面导入/覆盖确认/无效SKU/超量警告/有效的多店拆分）
- 本地预览：http://localhost:3000/#/product-apply/create

## CR-20260701-002 Excel/CSV 文件导入补充

### 依赖
- 新增 `xlsx` (SheetJS) 依赖到 `vue-reference/package.json`

### 文件变更
- `vue-reference/src/mocks/index.ts`：新增 `parseFileImport(file: File)` 异步函数，支持 CSV（纯文本解析）和 Excel（xlsx 库解析），第1行表头跳过，返回逗号分隔文本行数组
- `vue-reference/src/views/work-order-budget/CreateView.vue`：
  - 导入弹窗新增文件上传入口（文本框下方，分隔线区隔）
  - "选择文件"按钮 + 文件名显示 + 格式提示
  - 文件选择后自动解析填入 textarea，复用现有粘贴导入全链路
  - 错误处理：格式不支持 / 仅表头无数据 / 解析失败

### 格式约定
- Excel (.xlsx/.xls)：第1行=表头（跳过），列顺序 A=SKU B=数量 C=专卖店编号，第1个sheet
- CSV (.csv)：第1行=表头（跳过），逗号分隔，3列同上
- 解析后填入 textarea，用户可预览编辑后再点"识别检查"

### 验证
- 构建通过（47模块，+1 来自 xlsx 依赖）
- 可用性自测通过：CSV 3条数据识别为2个店 → 导入成功；Excel 同样数据 → 识别检查成功

## CR-20260701-002 模板下载功能

### 文件变更
- `vue-reference/src/mocks/index.ts`：新增 `downloadImportTemplate()` 函数，生成 .xlsx 模板（表头"产品SKU,数量,专卖店编号"+ 2行示例数据），触发浏览器下载
- `vue-reference/src/views/work-order-budget/CreateView.vue`：导入弹窗文件上传区新增"下载模板"链接（`margin-left: auto` 靠右），调用 `downloadImportTemplate()`

### 验证
- 构建通过（47模块）
- 点击"下载模板" → 浏览器下载 `批量导入模板.xlsx`，内容验证：Row1=表头, Row2=SKU001/5/31692, Row3=SKU002/3/31441

## CR-20260701-002 模板下载功能（修复）

### 修复内容
- 字段顺序区域醒目化：`CreateView.vue` 导入弹窗提示区改为浅橙色背景 + 橙色边框，"字段顺序：产品SKU，数量，专卖店编号" 独占一行、加粗橙色、字号加大
- 下载模板跳转问题：把 `<a href="#" @click.prevent>` 改成 `<button type="button">`，并在 `downloadImportTemplate(event)` 函数内显式 `event.preventDefault()` + `event.stopPropagation()`，彻底避免 hash 路由跳转

### 文件变更
- `vue-reference/src/views/work-order-budget/CreateView.vue`：提示区样式 + 下载模板按钮化
- `vue-reference/src/mocks/index.ts`：`downloadImportTemplate` 增加 event 参数阻止默认事件

### 验证
- 构建通过（47模块）
- 点击"下载模板"后页面 URL 保持 `#/product-apply/create` 不变
- 文件下载成功，内容正确

## CR-20260701-002 内容

### 改名 + 模板格式修复
- 全部"产品SKU" → "产品编号"：页面提示、字段顺序、错误信息、模板表头
- 模板格式修复：改用 CSV（UTF-8 BOM）替代 xlsx。原因：SheetJS 社区版不支持列宽和单元格样式写入，node-xlsx 同样丢失列宽；CSV 在 Excel/WPS 中自动适配列宽，中文通过 BOM 正确显示
- `downloadImportTemplate` 生成 CSV 文件（批量导入模板.csv），含 BOM + 表头 + 2 行示例
- 移除了 xlsx-populate 和 node-xlsx（未使用），保留 xlsx 用于文件导入解析
- 构建通过（47模块，体积从587KB降到497KB）

## CR-20260701-002 模板下载扩展名确认

### 用户反馈
- 截图显示保存对话框"保存为：批量导入模板"，未见扩展名

### 排查结论
- 当前代码 `a.download = '批量导入模板.csv'`，文件名已带 `.csv` 扩展名
- playwright-cli 实测下载文件为 `.playwright-cli/批量导入模板.csv`，扩展名正确
- 截图中未显示扩展名，系 macOS 保存对话框默认隐藏扩展名（可在 Finder 偏好设置中开启"显示所有文件扩展名"）

### 验证
- 重新构建并重启 8765 预览服务
- playwright-cli 点击"下载模板" → 事件日志显示 `Downloaded file 批量导入模板.csv`
- 文件系统确认：`批量导入模板.csv`（69 字节），扩展名正确

## CR-20260701-002 文档回写补漏（15:53）

- 用户指出变更清单 7.1 必改文档 4 份均未更新
- 已完成同步回写：
  - `产品申请工单AI开发任务说明.md` v0.2 → v0.3：6.1.1 补充批量导入 9 条规则
  - `产品申请工单字段清单初稿.md` v0.4 → v0.5：新增 2.8 批量导入字段与规则
  - `产品申请工单页面结构草图说明.md` v0.4 → v0.5：新增 5.6 批量导入弹窗结构
  - `产品申请工单和产品申请预算核销PRD.md` v0.5 → v0.6：新增 7.8 整单级批量导入
- 所有 4 份文档均包含 CR-20260701-002 更新日志 + 变更摘要 + 参考文档清单
- 开发总结关联文档表标注同步状态
- 日报遗留问题 #5 标记已完成
